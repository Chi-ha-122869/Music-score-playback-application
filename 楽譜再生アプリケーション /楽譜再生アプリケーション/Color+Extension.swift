import SwiftUI

extension Color {
    // メインカラー
    static let appOrange = Color(red: 1.0, green: 0.55, blue: 0.0)
    static let appDeepBlue = Color(red: 0.05, green: 0.15, blue: 0.35)
    
    // アクセント / サブ
    static let appAccent = Color(red: 0.7, green: 0.95, blue: 0.5)
    static let appLightGray = Color(red: 0.9, green: 0.9, blue: 0.95)
    static let appDarkGray = Color(red: 0.2, green: 0.2, blue: 0.25)
}

